<!-- #Difference between the master

1. Added a button to change to Celcius to Farenheit
2. Hover animation to Weather Channel
3. Hover animations to featured destionations
4. Validation logic is slightly changed
    if(name && contanct && email are good)
      display success
    else
      if(any one of the field is correct)
        no need to display error, for the correct field and the input field is not cleared (name is correct)
      else if(any of the feild is incorrect)
        display error, field is cleared
      else
        display error for all field
        -->
